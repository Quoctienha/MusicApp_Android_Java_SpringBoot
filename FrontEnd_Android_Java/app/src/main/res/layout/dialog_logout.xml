<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1C2526"
    android:padding="16dp">

    <!-- Title: "Logout" -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Logout"
            android:textSize="18sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />

        <!-- Close button (using ImageButton with icon) -->
        <ImageButton
            android:id="@+id/btn_close_dialog"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:background="@android:color/transparent"
            android:contentDescription="Close" />
    </LinearLayout>

    <!-- Message: "Are you sure you want to log out of Musico?" -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Are you sure you want to log out of Musico?"
        android:textSize="16sp"
        android:textColor="#BBBBBB"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="24dp" />

    <!-- Logout Button -->
    <Button
        android:id="@+id/btn_confirm_logout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:textSize="16sp"
        android:background="@android:color/transparent"
        android:textColor="#FFFFFF"
        android:paddingVertical="12dp"
        android:layout_gravity="center" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#555555" />

</LinearLayout>